#include "main.h"
#include <stdio.h>

/**
 * factorial - check the code
 * @i: number
 * Return: f of n
 */
int factorial(int i)
{
	if (i < 0)
	{
		return (-1);
	}
	if (n == 0)
	{
		return (1);
	}
	return (n * factorial(n - 1));
}
