#include "main.h"
/**
 * main - writes the character c to stdout
 * Return: On success 1.
 */
int main(void)
{
	_puts("\"At the end of the day, my goal was to be the best hacker\"\n\t - Kevin Mitnick");
	return(0);
}
